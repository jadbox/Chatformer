// JavaScript Document
function isAuth() {
	return false;
}
function temp() {
	var chatter = [
	"hello there",
	"so welcome!"
	];
	delayed_convo(chatter);
	//delayed_stream("So welcome!");
}